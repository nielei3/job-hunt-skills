# Which company/project profile ai-chrome runs under ON THIS MACHINE.
# → profiles/<value>.md   (see SKILL.md → "Project profile")
#
# Copy this file to `.env` and set the profile you created under profiles/.
# No credentials go here.
AI_CHROME_PROFILE=example
